import type { BudgetLimit, Category, DashboardSummary } from "../types/finance";
import { formatCurrency, getPeriodLabel } from "../utils/format";
import EmptyState from "../components/EmptyState";
import Icon from "../components/Icon";
import PageHeader from "../components/PageHeader";
import Panel from "../components/Panel";

export default function BudgetsPage({year,month,summary,limits,expenseCategories,onEditBudget,onAddLimit,onDeleteLimit}:{year:number;month:number;summary:DashboardSummary;limits:BudgetLimit[];expenseCategories:Category[];onEditBudget:()=>void;onAddLimit:()=>void;onDeleteLimit:(categoryId:number)=>void;}){
  return <><PageHeader eyebrow="Enveloppes" title="Budgets" description={`Pilote ton budget global et tes limites par catégorie pour ${getPeriodLabel(year,month)}.`} action={<div className="flex gap-2"><button onClick={onEditBudget} className="rounded-2xl border border-slate-200 bg-white px-4 py-3 text-sm font-black text-slate-700 dark:border-slate-700 dark:bg-slate-900 dark:text-white">Budget global</button><button onClick={onAddLimit} disabled={expenseCategories.length===0} className="rounded-2xl bg-[var(--accent)] px-4 py-3 text-sm font-black text-white disabled:opacity-50">+ Limite catégorie</button></div>}/><div className="grid gap-5 xl:grid-cols-[.75fr_1.25fr]"><Panel className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-slate-400">Budget global</p><p className="mt-2 text-4xl font-black tracking-tight text-slate-950 dark:text-white">{formatCurrency(summary.budget)}</p></div><div className="grid size-12 place-items-center rounded-2xl bg-[var(--accent-soft)] text-[var(--accent)]"><Icon name="budget" className="size-5"/></div></div>{summary.budget!==null?<><div className="mt-8 h-3 overflow-hidden rounded-full bg-slate-100 dark:bg-slate-800"><div className="h-full rounded-full bg-[var(--accent)]" style={{width:`${Math.min(Number(summary.percentage_used??0),100)}%`}}/></div><div className="mt-4 grid grid-cols-2 gap-3"><div className="rounded-2xl bg-slate-50 p-4 dark:bg-slate-950"><p className="text-xs font-bold text-slate-400">Dépensé</p><p className="mt-1 font-black text-slate-900 dark:text-white">{formatCurrency(summary.expenses)}</p></div><div className="rounded-2xl bg-slate-50 p-4 dark:bg-slate-950"><p className="text-xs font-bold text-slate-400">Restant</p><p className={`mt-1 font-black ${Number(summary.remaining)>=0?"text-slate-900 dark:text-white":"text-rose-600"}`}>{formatCurrency(summary.remaining)}</p></div></div></>:<p className="mt-8 text-sm leading-6 text-slate-500 dark:text-slate-400">Définis un budget global pour visualiser immédiatement ton reste à dépenser.</p>}</Panel><Panel className="overflow-hidden"><div className="border-b border-slate-100 px-6 py-5 dark:border-slate-800"><p className="text-xs font-bold uppercase tracking-[0.16em] text-slate-400">Budgets ciblés</p><h2 className="mt-1 text-lg font-black text-slate-950 dark:text-white">Limites par catégorie</h2></div>{limits.length===0?<EmptyState title="Aucune limite par catégorie" description="Ajoute une enveloppe pour les courses, loisirs, abonnements ou toute autre catégorie."/>:<div className="divide-y divide-slate-100 dark:divide-slate-800">{limits.map(limit=><div key={limit.id} className="px-6 py-5"><div className="flex items-start gap-4"><span className="mt-1 size-3 rounded-full" style={{backgroundColor:limit.category_color}}/><div className="min-w-0 flex-1"><div className="flex items-center justify-between gap-3"><div><p className="font-black text-slate-900 dark:text-white">{limit.category_name}</p><p className="mt-1 text-xs text-slate-400">{formatCurrency(limit.spent)} sur {formatCurrency(limit.amount)}</p></div><div className="flex items-center gap-3"><span className={`text-sm font-black ${Number(limit.remaining)>=0?"text-slate-800 dark:text-slate-100":"text-rose-600"}`}>{formatCurrency(limit.remaining)}</span><button onClick={()=>onDeleteLimit(limit.category_id)} className="grid size-8 place-items-center rounded-xl text-slate-400 hover:bg-rose-50 hover:text-rose-600 dark:hover:bg-rose-950/30"><Icon name="trash" className="size-4"/></button></div></div><div className="mt-3 h-2 overflow-hidden rounded-full bg-slate-100 dark:bg-slate-800"><div className={`h-full rounded-full ${Number(limit.percentage_used??0)>100?"bg-rose-500":"bg-[var(--accent)]"}`} style={{width:`${Math.min(Number(limit.percentage_used??0),100)}%`}}/></div></div></div></div>)}</div>}</Panel></div></>;
}
